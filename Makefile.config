################################################################
# Makefile.in : compiling options for the 		       #
# Makefile for GADGET and AHF related reading routines,        #
# growth factor calculations, integrations and interpolations  #
################################################################

# Use the new vevsion of AHF
OPT    += -DAHF_v1
# Convert units from kpc to Mpc
OPT    += -DUSE_UNIT_MPC
# Print virial theorem results for the halos
#OPT    += -DVIRIAL_THEOREM
# Print additional informations
OPT	+= -DPRINT_INFO
# Catalogue - reading options
#OPT	+= -DGAS
#OPT	+= -DEXTRA_GAS
# Don't use nor search data from the simulations
#OPT	+= -DTH_ONLY


SYS="laptop"
#SYS="comodo"
#SYS="castor"
#SYS="popia"

CC      = /usr/bin/gcc
CFLAGS  =

ifeq (${SYS}, "laptop")
#CINCL    = -I. -I${TAREADOR_HOME}/valgrind_module/tareador/lib_valgrind_annotations
#CLIBS    = -L. ${TAREADOR_HOME}/valgrind_module/tareador/lib_valgrind_annotations/tareador_hooks.o

CFLAGS  += -lgsl -lgslcblas -lm -lrt
CFLAGS  += -Wall ${CLIBS}
INCLUDE  = -I/usr/local/include ${CINCL}
BASELIBS = -L/usr/local/lib 
HOME_DIR = /home/edoardo/Analysis/
endif

ifeq (${SYS}, "castor")
CFLAGS  += -L/home/carlesi/libs/gsl/lib
CFLAGS  += -L/home/carlesi/libs/lib
#CFLAGS	+= -ldrfftw -ldfftw
INCLUDE = -I/home/carlesi/libs/include
BASELIBS = -L/home/carlesi/libs/lib
HOME_DIR=/home/carlesi/Analysis/
endif

ifeq (${SYS}, "comodo")
HOME_DIR=/home/edoardo/Analysis/
CFLAGS  += -lgsl -lgslcblas -lm
CFLAGS  += -L/home/edoardo/libs/lib
#CFLAGS	+= -ldrfftw -ldfftw
INCLUDE = -I/usr/local/include 
INCLUDE += -I/home/edoardo/libs/include
BASELIBS = -L/usr/local/lib 
BASELIBS += -L/home/carlesi/libs/lib
endif
